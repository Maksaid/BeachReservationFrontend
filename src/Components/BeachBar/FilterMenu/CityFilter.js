import React from 'react';

const CityFilter = () => {
    return(
        <select>
            <option value="Afghanistan">Afghanistan</option>
            selectCity</select>
    );
};
export default CityFilter;
